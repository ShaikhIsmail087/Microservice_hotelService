server:
  port: 8082

spring:
  datasource:
    url: jdbc:oracle:thin:@localhost:1521/xe
    username: system
    password: system
    driver-class-name: oracle.jdbc.OracleDriver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true